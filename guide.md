# 보안회사 웹사이트 (인라인 제작) — 실습 가이드

오늘은 GitHub Copilot **인라인 채팅**(Ctrl+I / Cmd+I) 기능으로 보안회사 원페이지 웹사이트를 직접 만들어봅니다. 섹션 하나씩 HTML을 만들고, 바로 CSS로 색을 입히는 순서를 5번 반복합니다.

> 💡 **인라인 채팅이 뭔가요?** 파일을 열어둔 채로 `Ctrl+I`(Mac은 `Cmd+I`)를 누르면 프롬프트 입력창이 그 자리에서 뜹니다. 타이핑만 하면 알아서 다음 코드를 예측해주는 "자동완성"과 달리, 인라인 채팅은 **"~를 만들어줘/바꿔줘"라는 지시를 직접 내리는 방식**입니다. 커서가 어디 있든, Copilot은 지금 열려 있는 **파일 전체**를 보고 의미상 맞는 위치(예: `<head>` 안, `</body>` 바로 앞)에 코드를 삽입하거나 수정합니다. 단, **다른 파일의 내용은 함께 고려하지 못합니다** — 그래서 오늘은 파일을 옮길 때마다 필요한 정보(클래스명 등)를 프롬프트에 다시 적어줍니다.

## 0️⃣ 시작하기 전에

- 이 저장소를 **Fork** → 로컬에 **Clone**
- **`dev` 브랜치를 만들고 그 위에서 작업합니다** (지난주와 동일한 3브랜치 전략)
  ```
  git checkout -b dev
  ```
- 파일 3개를 미리 만들어둡니다: `index.html`, `style.css`, `script.js`
- VS Code에서 GitHub Copilot이 켜져 있는지 확인 (우측 하단 아이콘)

> 💡 오늘 만드는 결과물은 완성 예시와 똑같을 필요 없습니다. 구조(섹션 5개)만 같으면 색상, 문구, 회사명은 자유롭게 바꿔도 됩니다.
>
> 🌿 **브랜치 전략 (지난주 복습)**
> - `main`: 원본 그대로 보존
> - `dev`: 오늘 실습 작업은 전부 여기서
> - `gh-pages`: 완성 후 배포용 (dev를 merge)

## 1️⃣ 기본 골격 만들기

섹션을 만들기 전에, 세 파일이 서로 연결된 기본 골격부터 준비합니다.

**STEP A. HTML 기본구조는 Emmet으로 (Copilot 아님)**

`index.html`을 열고 빈 첫 줄에 느낌표(`!`)를 입력한 뒤 **Tab**(또는 Enter)을 누르세요. VS Code의 **Emmet** 기능이 HTML5 기본 구조(`<!DOCTYPE html>`부터 `<body>`까지)를 한 번에 채워줍니다. 이건 Copilot이 아니라 VS Code 자체 기능이라, 매번 AI에게 요청할 필요 없이 습관처럼 쓰면 됩니다.

```
!  →  Tab
```

- [ ] `!` + Tab으로 HTML5 기본구조가 채워졌다

**STEP B. CSS/JS 파일 연결은 인라인 채팅으로 요청**

기본구조가 채워졌다면, `Ctrl+I`로 인라인 채팅을 열어 `style.css`, `script.js`를 연결하는 코드를 요청합니다. 커서 위치와 무관하게, Copilot이 파일 전체를 보고 `<head>`와 `</body>` 앞 등 적절한 위치에 알아서 넣어줍니다.

**프롬프트 예시**
```
이 HTML 파일에 style.css를 head에서, 
script.js를 body 끝에서 각각 연결해줘.
```

- [ ] `<link rel="stylesheet" href="style.css">`가 head 안에 있다
- [ ] `<script src="script.js">`가 body 끝에 있다

이제 STEP 1(Nav)부터 섹션 작업을 시작합니다.

## 2️⃣ 오늘 만드는 것

```
nav (앵커링크: Hero / Services / About / Contact)
 └─ hero (회사 슬로건 + 버튼)
 └─ services (서비스 카드 3개, 각 카드에 "자세히 보기" 링크)
 └─ about (회사 소개)
 └─ contact/footer (연락처)
```

## 3️⃣ 진행 방식 — "HTML 만들고 → 바로 CSS 입히기"를 5번 반복

인라인 채팅은 **지금 열려 있는 파일 전체**를 보고 코드를 제안합니다(다른 파일 내용까지는 함께 보지 못합니다). 그래서 섹션 하나마다 이렇게 진행합니다.

1. `index.html`에서 그 섹션의 마크업을 인라인 채팅으로 생성
2. `style.css`로 파일을 옮겨서 (index.html 탭은 닫지 말고 열어두세요!) 방금 만든 섹션에 스타일을 입힘
3. 브라우저로 결과 확인

> ⚠️ **중요**: CSS를 요청할 때 방금 만든 **클래스명을 프롬프트에 꼭 적어주세요.** 파일을 옮기면 Copilot이 방금 만든 마크업을 정확히 기억하지 못할 수 있습니다. (예: "`.service-card` 클래스에 스타일을 입혀줘") 클래스명 없이 요청하면 Copilot이 없는 클래스명을 지어내는 경우도 있으니, 결과가 이상하면 클래스명이 실제 HTML과 일치하는지부터 확인하세요.

### STEP 1. Nav (헤더/네비게이션)

**HTML 프롬프트 예시**
```
헤더에 로고 텍스트("SecureCore")와 4개의 앵커링크 
(Hero, Services, About, Contact)를 가진 
네비게이션 바 마크업을 만들어줘.
클래스명은 nav, .logo, .nav-links로 해줘.
```

**CSS 프롬프트 예시** (style.css로 이동 후)
```
nav, .logo, .nav-links 클래스에 스타일을 입혀줘.
가로 배치, 오른쪽 정렬 메뉴, 어두운 배경에 흰 글씨로.
```

- [ ] Nav 완성, 4개 앵커링크 클릭 시 해당 섹션으로 이동 확인 (섹션이 아직 없어도 링크 자체는 미리 만들어둡니다)

### STEP 2. Hero

**HTML 프롬프트 예시**
```
회사 슬로건과 "무료 상담 신청" 버튼이 있는 
hero 섹션 마크업을 만들어줘.
클래스명은 .hero, .hero-title, .cta-button으로 해줘.
```

**CSS 프롬프트 예시**
```
.hero 섹션을 화면 전체 너비로, 
가운데 정렬로 배치하고 배경색을 짙은 남색으로 해줘.
.cta-button은 눈에 띄는 색으로.
```

- [ ] Hero 완성, 버튼과 슬로건이 화면 중앙에 보임

### STEP 3. Services

**HTML 프롬프트 예시**
```
3개의 서비스 카드를 가진 섹션 마크업을 만들어줘.
각 카드는 아이콘, 제목, 한 줄 설명, "자세히 보기" 링크를 포함해.
서비스: 취약점 진단, 모의해킹, 보안 컨설팅.
클래스명은 .services, .service-card로 해줘.
```

**CSS 프롬프트 예시**
```
.services 안의 .service-card 3개를 가로로 나란히 배치하고,
hover 시 살짝 떠오르는 효과를 줘.
```

- [ ] Services 완성, 카드 3개가 가로로 나란히 보임
- [ ] (선택) "자세히 보기" 링크를 눌러 별도 서브페이지로 연결해보기 — 시간이 남으면 도전!

### STEP 4. About

**HTML 프롬프트 예시**
```
회사 소개 문단과 설립연도, 핵심 가치 3가지를 담은 
about 섹션 마크업을 만들어줘.
클래스명은 .about, .about-values로 해줘.
```

**CSS 프롬프트 예시**
```
.about 섹션에 여백을 넉넉하게 주고, 
.about-values는 아이콘과 함께 가로로 배치해줘.
```

- [ ] About 완성

### STEP 5. Contact/Footer

**HTML 프롬프트 예시**
```
이메일, 전화번호(예시 값), 주소(예시 값)와 
저작권 문구가 있는 contact/footer 섹션 마크업을 만들어줘.
클래스명은 .contact, .footer로 해줘.
```

**CSS 프롬프트 예시**
```
.contact와 .footer에 어두운 배경, 
연락처 정보는 세로로 정리해서 보여줘.
```

- [ ] Contact/Footer 완성
- ⚠️ 실제 개인 이메일·전화번호는 넣지 마세요. 예시 값(`example@securecore.com` 등)으로 채웁니다.

## 4️⃣ JS 인터랙션 추가

5개 섹션이 모두 끝났다면, `script.js`로 이동해서 가벼운 인터랙션을 하나 추가합니다.

**프롬프트 예시**
```
nav 안의 .nav-links 메뉴를, 화면이 좁아지면 
햄버거 아이콘으로 바꾸고 클릭 시 펼쳐지는 토글 기능을 
만들어줘.
```

- [ ] 화면을 좁혀서(모바일 크기) 메뉴가 토글되는지 확인

## 5️⃣ 자유 수정 (개인화)

5개 섹션 + JS까지 다 만들었다면, 이제 자기만의 버전으로 꾸며봅니다. 오늘도 계속 **인라인 채팅**을 사용합니다.

- 색상 테마 바꾸기 → `style.css`를 열고 인라인 채팅으로 "이 사이트의 색상 팔레트를 ~ 계열로 바꿔줘"처럼 요청
- 회사명, 슬로건, 서비스 내용 자기 아이디어로 바꾸기 → `index.html`에서 인라인 채팅으로 요청
- 레이아웃 살짝 조정하기 → 바꾸고 싶은 부분이 있는 파일을 열고 그 파일 안에서 요청

> 💡 색상 하나를 바꾸는데도 CSS, 어떤 땐 HTML까지 파일을 오가며 여러 번 요청해야 할 수 있습니다. 이 번거로움을 기억해두세요 — 잠시 후 Plan 모드로 같은 작업을 해보면 이 부분이 어떻게 달라지는지 비교하게 됩니다.

## 6️⃣ 배포 (GitHub Pages)

지난주와 동일한 방식으로 배포합니다.

1. `dev` 브랜치에서 작업 내용을 커밋
2. `gh-pages` 브랜치를 만들고 `dev`를 merge
   ```
   git checkout -b gh-pages
   git merge dev
   git push origin gh-pages
   ```
3. 저장소 **Settings → Pages**에서 배포 브랜치를 `gh-pages`로 확인
4. 배포된 URL 접속해서 정상 동작 확인

## 7️⃣ 제출 (Pull Request)

1. 본인 저장소에서 `dev` 브랜치로 이동한 뒤 **Contribute** → **Open pull request**
2. 원본 저장소(`hitech26-w4-web-inline`)의 `main` 브랜치로, **`dev` 브랜치 기준**으로 PR
3. 제목에 이름 포함 (예: `[홍길동] 보안회사 웹사이트(인라인) 제출`)

> 💡 이 PR은 실제로 Merge되지 않는 제출용입니다.

## ✅ 오늘 완성 체크

- [ ] `dev` 브랜치를 만들고 그 위에서 작업했다
- [ ] `!` + Tab으로 HTML 기본구조를 만들었다
- [ ] `style.css`, `script.js`가 `index.html`에 연결되어 있다
- [ ] `index.html` / `style.css` / `script.js` 3개 파일로 구성했다
- [ ] 5개 섹션(nav/hero/services/about/contact)이 모두 있다
- [ ] JS 인터랙션이 최소 1개 동작한다
- [ ] 나만의 색상/문구로 개인화했다
- [ ] GitHub Pages로 배포하고 링크 접속을 확인했다
- [ ] `dev` 브랜치 기준으로 조직 저장소에 PR을 제출했다

## ❓ 자주 묻는 질문 FAQ

### Q1. `!` + Tab을 눌렀는데 아무 반응이 없어요
**A**: 파일 확장자가 `.html`로 저장되어 있는지 확인하세요. Emmet은 파일 타입을 보고 동작하기 때문에, 확장자가 없거나 다르면 작동하지 않습니다.

### Q2. CSS/JS 파일을 연결했는데 브라우저에 스타일이 하나도 안 보여요
**A**: `<head>` 안의 `<link>` 경로와 실제 파일명(`style.css`)이 정확히 일치하는지, 오타는 없는지 확인하세요.

### Q3. CSS를 요청했는데 스타일이 하나도 안 먹어요
**A**: 클래스명 철자가 HTML과 CSS에서 다를 수 있습니다. `index.html`을 열어서 실제 클래스명을 확인하고, 그 이름을 CSS 프롬프트에 정확히 다시 적어서 요청해보세요.

### Q4. Copilot이 없는 클래스나 태그를 만들어냈어요
**A**: 인라인 채팅은 열려 있는 파일 안에서는 전체를 보고 판단하지만, **다른 파일의 내용은 함께 보지 못합니다.** CSS 파일로 넘어가면 방금 만든 HTML의 클래스명을 모를 수 있으니, `index.html` 탭을 닫지 말고 열어둔 채로 진행하고, 프롬프트에 클래스명을 항상 명시하세요.

### Q5. 앵커링크(`#services` 등)를 눌러도 이동이 안 돼요
**A**: 그 섹션의 `id` 속성이 링크의 `href="#services"`와 정확히 일치하는지 확인하세요. `<section id="services">`처럼 id가 있어야 합니다.

### Q6. "자세히 보기" 서브페이지는 꼭 만들어야 하나요?
**A**: 아니요, 선택 사항입니다. 시간이 남으면 도전해보세요.

---

**📚 수고하셨습니다! 잠시 후 같은 사이트를 Plan 모드로 다시 만들어보며 비교합니다 → [`hitech26-w4-web-plan`](https://github.com/kopo-kangseo-cybersec/hitech26-w4-web-plan)**